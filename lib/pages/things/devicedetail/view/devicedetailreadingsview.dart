import 'package:flutter/material.dart';

class DeviceDetailReadingsView extends StatelessWidget {
  const DeviceDetailReadingsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('DeviceDetailChartsView'),
    );
  }
}
